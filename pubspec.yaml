name: explorador_de_valles
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Base de datos local
  sqflite: ^2.3.3
  sqflite_common_ffi: ^2.3.0
  path: ^1.9.0

  # Sensores (para detectar inclinación)
  sensors_plus: ^7.0.0

  # Vibración háptica moderna
  flutter_vibrate: ^1.3.0

  # Evita que la pantalla se apague durante el juego
  wakelock_plus: ^1.2.5

  # Reproducción de sonidos
  audioplayers: ^6.0.0

  # Almacenamiento local de preferencias
  shared_preferences: ^2.2.2

  # Iconos de iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/inicio.mp3
    - assets/sounds/success.mp3
    - assets/sounds/fail.wav
    - assets/images/animales/
    - assets/images/futbol/
